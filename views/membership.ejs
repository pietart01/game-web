<!DOCTYPE html>
<html lang="ko">
<head>
  <%- include('partials/head') %>
  <link rel="stylesheet" href="/css/membership.css">
</head>
<body>
  <%- include('partials/navbar') %>

  <div class="content">
    <h2 class="section-title">멤버십</h2>

    <div class="membership-cards">
      <% memberships.forEach(membership => { %>
        <div class="membership-card <%= membership.id %>">
          <img src="<%= membership.imageUrl %>"
               alt="<%= membership.id %> 멤버십"
               class="membership-badge">
          <h3 class="membership-title">
            <span class="tier"><%= membership.membershipName %> 멤버십</span>
          </h3>

          <div class="benefit-row">
            <span class="benefit-label">회득경험치</span>
            <span class="benefit-value"><%= membership.earnedExp %>% 적립</span>
          </div>

          <div class="benefit-row">
            <span class="benefit-label">보너스머니</span>
            <span class="benefit-value">
        <%= membership.bonusMoney.toLocaleString() %> 골드
      </span>
          </div>

          <div class="benefit-row">
            <span class="benefit-label">월 이용료</span>
            <span class="benefit-value">
        <%= membership.monthlyFee.toLocaleString() %> 캐시
      </span>
          </div>

          <button class="subscribe-button">멤버십 가입하고 혜택받기</button>
        </div>
      <% }); %>
<!--      <%- include('partials/membership/premium-card') %>-->
<!--      <%- include('partials/membership/vip-card') %>-->
    </div>

    <%- include('partials/membership/terms') %>
  </div>

  <%- include('partials/footer') %>
  <script type="module" src="/js/modal.js"></script>
  <script type="module">
    import { initProfileModal } from '/js/profileModal.js';
    import { initCashChargeModal } from '/js/cash-charge-modal.js';

    document.addEventListener('DOMContentLoaded', () => {
      initProfileModal();
      initCashChargeModal();
    });
  </script>

</body>
</html>
